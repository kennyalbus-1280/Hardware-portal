# Ecommerce Project Structure

- public/ contains storefront entry points
- app/ contains bootstrap and shared application logic
- admin/ remains the admin area
- includes/ contains shared layouts
- config/ contains runtime configuration
